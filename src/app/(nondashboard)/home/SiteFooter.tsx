// Deprecated: SiteFooter has been merged into the global components/Footer.tsx.
// This wrapper is kept temporarily to avoid import errors; remove after ensuring no references remain.
import React from 'react';
import Footer from '@/components/Footer';

export default function SiteFooter() {
  return <Footer />;
}
